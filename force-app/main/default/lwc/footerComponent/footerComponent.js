import { LightningElement } from 'lwc';

import linkedinIcon from '@salesforce/resourceUrl/linkedIn';
import igIcon from '@salesforce/resourceUrl/instagram';
import youtubeIcon from '@salesforce/resourceUrl/Youtube';
import twitterxIcon from '@salesforce/resourceUrl/xTwitter';

export default class FooterComponent extends LightningElement {

    linkedinIconUrl = linkedinIcon; // URL del recurso estático
    instagramIconUrl = igIcon; // URL del recurso estático
    youtubeIconUrl = youtubeIcon; // URL del recurso estático
    twitterXIconUrl = twitterxIcon; // URL del recurso estático


}